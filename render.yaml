# Render Blueprint: Express API (not the repo root — no yarn start there).
# Dashboard: https://render.com/docs/deploys#root-directory
services:
  - type: web
    name: tourist-node-api
    runtime: node
    plan: free
    rootDir: src/nodejs-backend
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
