## TravelVerse 3D

Mountain Explorers travel experience — frontend and Internet Computer backend.

### Documentation

- **[Phase 1 — product rules](docs/PHASE1-PRODUCT-RULES.md)** — glossary, package/category rules, required fields, pricing and batch behavior (baseline before catalog schema work).

### Local development

Use `pnpm` from the repository root. Set `STORAGE_GATEWAY_URL` when you need blob storage (see `deploy.sh` for a local example).
